// Conditional operators = && and || use

let myAge = 17;
let myIncome = 100000;

// using && 
if(myAge >= 15 && myIncome > 50000){
    console.log("you can join the world developer squad");
}

// using ||
if(myAge >= 18 || myIncome > 50000){
    console.log("OK let's start the proces, we will manage it if you don't have tow condition")
}